
log("Hello, Currant Comparisons!");

// comparisons

// objects (and their member functions)
a: Box = Box(69u8);

// copy of an object
b: Box = a;
log(a == b); // -> true

// different object
log(a == log); // -> false

// object with same values
log(a == Box(69u8)); // -> true

// object with different values
log(a == Box(32u16)); // -> false

// functions imported from javascript
log(f@currantLog == f@currantLog); // -> true
log(f@currantLog == f@currantSqrt); // -> false